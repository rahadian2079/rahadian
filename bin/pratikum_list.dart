void main() {
  // List
  List<int> numbers = [1, 2, 3, 4, 5];

  // Menambahkan elemen ke List
  numbers.add(6);

  // Mengakses elemen List
  print(numbers[0]);

  // Iterasi List menggunakan for-in
  for (int number in numbers) {
    print(number);
  }
}
